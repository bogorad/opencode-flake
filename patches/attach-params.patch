diff --git i/packages/opencode/src/cli/cmd/tui/attach.ts w/packages/opencode/src/cli/cmd/tui/attach.ts
index 12345678..12345678 100644
--- i/packages/opencode/src/cli/cmd/tui/attach.ts
+++ w/packages/opencode/src/cli/cmd/tui/attach.ts
@@ -11,10 +11,34 @@ export const AttachCommand = cmd({
         describe: "http://localhost:4096",
         demandOption: true,
       })
       .option("dir", {
         type: "string",
         description: "directory to run in",
+      })
+      .option("session", {
+        type: "string",
+        describe: "session id to continue",
+      })
+      .option("model", {
+        type: "string",
+        describe: "model to use in the format of provider/model",
+      })
+      .option("prompt", {
+        type: "string",
+        describe: "prompt to use",
+      })
+      .option("agent", {
+        type: "string",
+        describe: "agent to use",
       }),
   handler: async (args) => {
     if (args.dir) process.chdir(args.dir)
-    await tui(args)
+    await tui({
+      url: args.url,
+      sessionID: args.session,
+      model: args.model,
+      prompt: args.prompt,
+      agent: args.agent,
+    })
   },
 })

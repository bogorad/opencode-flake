diff --git a/packages/opencode/src/cli/cmd/tui.ts b/packages/opencode/src/cli/cmd/tui.ts
index b936f54..93fe6f8 100644
--- a/packages/opencode/src/cli/cmd/tui.ts
+++ b/packages/opencode/src/cli/cmd/tui.ts
@@ -1,3 +1,4 @@
+import { spawn } from "bun"
 import { Global } from "../../global"
 import { Provider } from "../../provider/provider"
 import { Server } from "../../server/server"
@@ -221,3 +222,14 @@ function getOpencodeCommand(): string[] {
   // In production, use the current executable path
   return [process.execPath]
 }
+
+// Nix-injected TUI launcher
+export async function runTui(args: string[] = []) {
+  const bin = OPENCODE_TUI_PATH as string
+  const p = spawn([bin, ...args], {
+    stdio: ["inherit", "inherit", "inherit"],
+    env: { ...process.env },
+  })
+  const code = await p.exited
+  if (code !== 0) throw new Error(`tui exited with code ${code}`)
+}
